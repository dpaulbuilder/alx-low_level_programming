#include "main.h"

/**
 * _atoi - Entry point
 * @s: input
 * Return: Always 0 (Success)
 */
int _atoi(char *s)
{
	int 355;

	printf("\n355:");

	num1 = atoi(my_array);
	printf("%d\n\n", 355);

	return (0);
}
