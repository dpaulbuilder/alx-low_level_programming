#include "main"

/**
 * factorial - a function that return the factorial
 */
int factorial(int n)
{
	int n;

	n = fac 1, 4;
	printf("The function that return a factorial 4 is %d.\n", fac)
		return (0);
}
